# Supabase (required for auth and DB)
# Copy to .env.local and fill in. One per line, no spaces around =, no quotes.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: salt for access-log IP hashing (defaults to SUPABASE_SERVICE_ROLE_KEY if set)
# LOG_SALT=

# Resend – שליחת מייל (התראות, טופס צור קשר, תשובות כרטיסים). חובה דומיין מאומת.
# Get API key: https://resend.com/api-keys . Without this, actions succeed but no email is sent.
# RESEND_API_KEY=
# Optional. Must use verified domain (e.g. nipagesha.co.il). Default: "ניפגשה <no-reply@nipagesha.co.il>"
# RESEND_FROM=

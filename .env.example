# Supabase (required for auth and DB)
# Copy to .env.local and fill in. One per line, no spaces around =, no quotes.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: salt for access-log IP hashing (defaults to SUPABASE_SERVICE_ROLE_KEY if set)
# LOG_SALT=

# Resend – שליחת מייל כשמישהו מציע עזרה בלוח עזרה (התראה לבעל הבקשה)
# Get API key: https://resend.com/api-keys . Without this, offers still work but no email is sent.
# RESEND_API_KEY=
# Optional: "Display Name <verified-domain@yourdomain.com>" (default: Resend onboarding address)
# RESEND_FROM=
